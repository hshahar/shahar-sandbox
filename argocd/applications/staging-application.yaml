# Staging Environment Application
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sha-k8s-blog-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    repoURL: https://github.com/yourusername/k8s-blog-platform.git  # Change to your repo
    targetRevision: HEAD
    path: helm/microservices-app
    helm:
      releaseName: sha-k8s-blog-staging
      valueFiles:
        - values-staging.yaml
      parameters:
        - name: environment
          value: "staging"
        - name: frontend.replicas
          value: "2"
        - name: backend.replicas
          value: "2"
        - name: postgresql.persistence.size
          value: "5Gi"
  
  destination:
    server: https://kubernetes.default.svc
    namespace: sha-staging
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  
  syncWave: 10
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
